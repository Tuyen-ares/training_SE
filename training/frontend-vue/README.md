# Vue frontend training

These standalone Vue examples are kept outside `apps/frontend/src` so they do
not become production routes or application dependencies.

- `examples/` contains the original training views.
- `components/` contains components used by those examples.

# Notification Outbox Remediation Checklist

## Control

- Overall status: **IN_PROGRESS**
- Allowed task states: `NOT_STARTED | IN_PROGRESS | BLOCKED | COMPLETE`
- Exactly one gap may be `IN_PROGRESS` at a time.
- A gap becomes `COMPLETE` only after its relevant tests pass and evidence is recorded below.
- This checklist is the detailed progress source. `implementation-memory.md` records only durable architecture decisions and phase-level verification.

## Gaps

| Gap | State | Work item | Acceptance criterion | Verification evidence |
| --- | --- | --- | --- | --- |
| GOV-01 | COMPLETE | Governance and documentation baseline | Guardrails exist in `AGENTS.md`; plan/memory no longer overstate implementation; baseline is recorded. | Documentation audit passed; see evidence log. |
| EVT-01 | COMPLETE | Typed and validated domain events | All 14 types map to required payloads; Zod rejects unsupported type/version/aggregate/IDs; invalid persisted events fail permanently. | Backend unit suite passed (89/89), including 14-event validation. |
| OBS-01 | COMPLETE | Three observers and dispatcher | Borrow request/history/asset issue observers return intents only; dispatcher aggregates subscribed observers and materializes only after all succeed. | Unit observer/failure tests and atomic DB materialization test pass. |
| MAT-01 | COMPLETE | Recipient, rich event templates, materializer boundaries and approval-summary delivery | Active permission resolution remains deduplicated; every notification event carries a compact business snapshot; one-off approval sends one delivery; Approve All sends one summary delivery; delivery rows reference JSON messages; no HTML delivery snapshot column is required; legacy payloads still render with fallback title/message. | Rich snapshot, approval-summary, schema, source, fallback-rendering and delivery/message-linkage checks passed. The forward migration is ready for deployment; the configured Aiven production datasource was not mutated. |
| REP-01 | COMPLETE | Outbox and recipient repositories | Runtime/dispatcher do not call Prisma; claim/release/fail/finalize enforce status and lease ownership; dispatch is atomic and idempotent. | Focused MariaDB two-claimant/non-owner/finalize test passes. |
| WFL-01 | COMPLETE | Business event transaction integrity | Rollback leaves no event; commit writes one event; approve-all emits only for changed details and one summary for successful bulk actions; normal/damaged return require relations and damaged return emits exactly two events; bulk handover emits one event per successful detail without partial detail state. | `pnpm --filter backend test` passed 89/89; `pnpm --filter backend test:db` passed 14/14, including grouped request pagination, bulk handover, duplicate protection, per-success history events and the approval summary snapshot. |
| HDL-01 | COMPLETE | Handler registry and delivery processor | Map registry rejects duplicates/missing channels; generic processor has no channel branch and applies outcomes/error policy. | Unit registry/processor policy tests pass. |
| HDL-02 | COMPLETE | Atomic in-app handler | Transaction re-locks/re-reads delivery; notification link and SENT are atomic; stale owner cannot write or leave an orphan. | MariaDB stale-reclaim test proves old owner creates no orphan and new owner creates one notification. |
| HDL-03 | COMPLETE | SMTP handler/provider | Only snapshot fields are used; deterministic Message-ID and provider ID persist; pooled transporter is reused; SMTP I/O occurs outside transactions. The generated RFC `Date` header explicitly uses the business timezone `Asia/Ho_Chi_Minh` (`+0700`) while database timestamps remain UTC. | `pnpm --filter backend exec tsx --test --test-concurrency=1 tests/notification-delivery.test.ts` passed 17/17, including the timezone header regression; backend typecheck passed; `git diff --check` passed. |
| RUN-01 | COMPLETE | Loops, concurrency, retry, cleanup | Recursive timeout loops expose `runOnce`; intervals/concurrency are applied (3/5/10, shared DB 3); six retries; cleanup is daily in batches of 500; timers are removed. | Unit limiter/retry/cleanup/timer tests pass. |
| RUN-02 | COMPLETE | SMTP failure policy and lifecycle | Disabled email skips; missing config leaves backlog; auth aborts batch and cools down 30s; transient/permanent classification works; shutdown stops claims and drains before closing dependencies. | Unit SMTP policy/cooldown/pending-I/O/shutdown tests pass. |
| DOC-01 | COMPLETE | Lifecycle and documentation consistency | Fourteen-event catalog, active MVP docs, plan and memory match code; F07 API/frontend unchanged; no FCM/SMS/future scope claims. | Source/doc audit passed; OpenAPI/frontend source unchanged; FCM/SMS remain explicitly future only. |
| VER-01 | COMPLETE | Final verification | Typecheck, build, unit, DB integration and verify-change dry-run/full pass; any required frontend build passes. | All final commands passed; see evidence log. |
| CLEAN-01 | COMPLETE | Readability formatting cleanup | Notification module and its focused tests use structured multiline TypeScript without changing runtime behavior. | Source review, typecheck, unit tests, verify dry/full and frontend build passed. |
| CFG-01 | COMPLETE | Remove SMTP reply-to configuration | `SMTP_REPLY_TO` is absent from environment documentation and runtime contracts; replies use `SMTP_FROM`. | Source audit, backend typecheck and 70/70 unit tests passed. |
| DOC-02 | COMPLETE | Newbie guide for notification queue tables | `docs/mq.md` explains every field, relationship, status and an end-to-end example for both new tables using the current Prisma schema. | Prisma schema, migration and runtime contract audit passed; `git diff --check` passed. |
| DOC-03 | COMPLETE | Reconcile constitution and deduplicate env example | Constitution describes the implemented outbox/worker architecture; notification worker batch/concurrency variables occur exactly once in `.env.example`. | Targeted source/link/count audit, backend typecheck and `git diff --check` passed. |
| SEC-01 | COMPLETE | Sanitize notification runtime errors | Authorization credentials, key/value secrets and URL credentials are redacted while ordinary messages remain readable and logs stay capped at 2,000 characters. | Backend unit suite passed (75/75), including credential and length cases. |
| CFG-02 | COMPLETE | Harden SMTP configuration states | Runtime distinguishes disabled, misconfigured and ready SMTP; invalid values never start the email loop and never expose configuration values. | Backend unit suite passed (75/75), including config states and loop selection. |
| OBS-02 | COMPLETE | Make observer ownership explicit | Three explicit observer lists cover every one of the 14 domain events exactly once. | Backend unit suite passed (89/89), including exact coverage/uniqueness. |
| REP-02 | COMPLETE | Bulk transactional queue claims | Outbox and delivery claims use one locking transaction and one bulk update per batch while preserving lease ownership and retry semantics. | Unit bulk-update and focused MariaDB concurrency/lease/attempt tests passed. |
| SCH-01 | COMPLETE | Remove notification schema fields proven redundant | `outbox_events.delivery_count` and delivery template provenance columns are removed through a forward migration; runtime behavior, retry, leases, snapshots and public F07 API remain unchanged. | Schema validate, generated client, typecheck, build, unit suite, local focused DB integration, local schema audit, Aiven migration deploy/status, Aiven read-only column audit and diff check passed. |
| DOC-04 | BLOCKED | Hardening documentation and final evidence | Notification spec and durable implementation memory match the verified runtime; evidence lists only commands actually run. | Automated gates and SMTP provider smoke passed; manual mailbox receipt confirmation remains. |
| CFG-03 | COMPLETE | Document planned RabbitMQ topology configuration | `.env.example` contains non-secret URL-based connection, exchange, queue, binding, prefetch, DLX and DLQ placeholders without claiming runtime support. | Key/source audit, backend typecheck and `git diff --check` passed; all twelve variables occur once and the URL remains blank. |
| CFG-04 | COMPLETE | Split planned RabbitMQ delivery topology by channel | `.env.example` enables the planned topology and defines independent IN_APP and EMAIL queues, topic bindings, prefetch values, dead-letter routing and DLQs; IN_APP remains the logical channel for a future Firebase realtime transport, without claiming current runtime support. | Targeted 19-key topology audit, backend typecheck, verify-change dry-run and `git diff --check` passed. |
| SCH-02 | COMPLETE | Generalize the application-assigned delivery message ID | A forward migration renames `smtp_message_id` to `outbound_message_id` without changing values, provider-ID behavior, outbox relationship, retry semantics or public APIs; active code/docs use the generic name and no provider-specific Teams column is added. | Source gates, Aiven migration deploy/status and read-only column audit passed; Aiven exposes only nullable `outbound_message_id VARCHAR(255)`. |

| MQ-01 | COMPLETE | RabbitMQ delivery transport | RabbitMQ transports notification_deliveries through channel-specific topic queues with confirm/return handling, publisher/consumer leases, CAS attempt accounting, retry/DLQ behavior and safe disabled/misconfigured/ready lifecycle states. | Focused contract, schema, EXPLAIN, live broker and MariaDB CAS integration checks pass. |

## Evidence log

| Date | Gap/phase | Command or review | Result | Residual risk |
| --- | --- | --- | --- | --- |
| 2026-08-21 | Baseline | `git status --short`; source/doc inspection | Existing outbox work is uncommitted. Current files show optional payloads, monolithic Prisma runtime, catalog observer, direct channel branching, and overstated docs. | Worktree changes are user-owned baseline and must be preserved. |
| 2026-08-21 | Baseline | Prior reported gates: backend typecheck/build/unit | Reported passing before remediation; not treated as current completion evidence. | DB/runtime concurrency and failure-policy coverage is incomplete. |
| 2026-08-21 | GOV-01 / Phase 0 | `rg` audit for `IMPLEMENTED`, separate worker/process, `start:worker`, active FCM, and premature handler claims | Passed for active notification plan/spec/memory. Plan is `REMEDIATION IN PROGRESS`; runtime is consistently one process; memory states the real gaps. | Older future roadmap sections remain explicitly non-active and will be reconciled in Phase 4. |
| 2026-08-21 | EVT-01 and unit baseline | `pnpm --filter backend typecheck`; `pnpm --filter backend test` | Passed; 66 tests, 0 failures. Covers typed validation, all 13 mappings, dispatcher errors, registry, retry/SMTP policy, snapshots, limiter, cleanup, recipients and templates. | Database lease/atomicity and full runtime behavior still require integration coverage. |
| 2026-08-21 | OBS/MAT/REP/HDL DB evidence | `pnpm --filter backend exec tsx --test --test-concurrency=1 tests/notification-outbox.integration.test.ts` | Passed. Proves one claimant, lease-owner enforcement, atomic two-channel materialization, inactive skips, stale owner rejection and no in-app orphan/duplicate. | SMTP is intentionally fake-provider tested; automated tests do not contact a real SMTP server. |
| 2026-08-21 | Existing DB baseline | `pnpm --filter backend test:db` | Passed before adding the focused outbox test: 13 tests, 0 failures, including borrow/issue lifecycle and F07 ownership/read behavior. | Full DB command must be rerun because the script now includes the new test and new lifecycle assertions. |
| 2026-08-21 | RUN-01/RUN-02 | `pnpm --filter backend test` | Passed after runtime additions: 70 tests, 0 failures. Covers pending SMTP concurrency, auth abort/cooldown, scheduler cleanup and graceful drain order. | Process-level signal handling remains covered by composition/source review rather than spawning a production child process. |
| 2026-08-21 | Final backend gates | `pnpm --filter backend typecheck`; `pnpm --filter backend build`; `pnpm --filter backend test`; `pnpm --filter backend test:db` | Passed. Unit: 70/70. DB: 14/14 including focused outbox and lifecycle event counts. | No real SMTP server is contacted; provider behavior is fake-tested and SMTP remains at-least-once. |
| 2026-08-21 | Final repository gates | `node <bigin-verify-change>/scripts/verify.mjs --dry-run`; `node <bigin-verify-change>/scripts/verify.mjs`; `git diff --check` | Selector chose backend typecheck and frontend build; both passed. Diff check passed after whitespace cleanup. | Frontend build reports the pre-existing large-chunk warning; no frontend source or public API changed. |
| 2026-08-21 | CLEAN-01 | Targeted source review; backend typecheck/unit; verify dry/full | Notification source and focused tests use structured multiline formatting. Typecheck, unit tests, selected backend/frontend gates and diff check passed. | No repository Prettier dependency or whole-backend formatting was introduced. |
| 2026-08-21 | CFG-01 | Source/doc audit; `pnpm --filter backend typecheck`; `pnpm --filter backend test` | `SMTP_REPLY_TO` removed from env example, active docs, provider, handler and composition. Typecheck and 70/70 tests passed. | Replies now use the `SMTP_FROM` mailbox according to SMTP client behavior. |
| 2026-08-21 | DOC-02 | Compared `docs/mq.md` with Prisma schema, migration, domain-event/template/materializer code; `git diff --check` | Passed. Both new tables, every column, statuses, indexes/FKs, SMTP IDs, snapshots and end-to-end examples are documented using current runtime values. | Documentation-only change; no runtime code or schema changed, so backend tests were not rerun. |
| 2026-08-21 | DOC-03 | Targeted old-claim/link/env-key audit; verify-change dry-run; `pnpm --filter backend typecheck`; `git diff --check` | Passed. Six obsolete event-bus/phase claims are absent; referenced docs exist; all five worker batch/concurrency keys occur once; backend typecheck and whitespace check passed. | Verify-change full run was not repeated because its frontend build selection comes from unrelated pre-existing worktree changes; no frontend/runtime code changed in this cleanup. |
| 2026-08-21 | SEC-01 / CFG-02 / OBS-02 / REP-02 unit | `pnpm --filter backend typecheck`; `pnpm --filter backend test` | Passed. Unit: 75/75; includes sanitization, SMTP states/loop selection, exact observer ownership and one bulk update for a 100-row batch. | MariaDB concurrency and `SKIP LOCKED` syntax still require integration verification. |
| 2026-08-21 | REP-02 focused DB | `pnpm --filter backend exec tsx --test --test-concurrency=1 tests/notification-outbox.integration.test.ts` | Passed after stopping the concurrently running local backend worker. Confirms MariaDB `FOR UPDATE SKIP LOCKED`, single-winner claims, stale reclaim, lease ownership and one attempt increment per claim. | Full DB suite must be rerun with the worker kept stopped. |
| 2026-08-21 | Hardening final backend gates | `pnpm --filter backend typecheck`; `pnpm --filter backend build`; `pnpm --filter backend test`; `pnpm --filter backend test:db` | Passed. Unit: 75/75. DB: 14/14 after isolating the local notification worker. | None for automated backend coverage. |
| 2026-08-21 | SMTP smoke | Restarted backend; created a new borrow approval through the API; polled its new EMAIL delivery | Observed `PENDING -> PROCESSING -> SENT`; `skip_reason` is null and `provider_message_id` is stored. | SMTP provider acceptance is verified; mailbox receipt requires manual confirmation because this workspace has no mailbox access. Historical skipped deliveries were not changed. |
| 2026-08-21 | Hardening repository gates | verify-change dry-run/full; `git diff --check` | Passed. Selector ran backend typecheck and frontend production build; diff check passed. | Frontend build retains its existing large-chunk warning. |
| 2026-08-25 | CFG-03 | Targeted English comment and `RABBITMQ_*` key audit; verify-change dry-run; backend typecheck; `git diff --check` | Added twelve non-secret RabbitMQ placeholders in three English-documented groups. The failed-message routing key and DLQ binding key are distinct settings; every key occurs once and `RABBITMQ_URL` is blank. | Configuration preparation only; no RabbitMQ publisher or consumer is implemented yet. |
| 2026-08-25 | SCH-01 partial | Prisma generate/validate; backend typecheck/build/unit; source audit; verify-change dry-run; `git diff --check`; `prisma migrate status` | Forward migration and source/docs are aligned; schema is valid; typecheck/build and 75/75 unit tests passed; removed fields have no current source/test/doc references. | Deploy and `test:db` were not run because the active datasource resolves to the Aiven `it_asset_tracking` database. Explicit production authority or a separate test database is required before destructive column drops and integration writes. |
| 2026-08-25 | SCH-01 Aiven deploy | `prisma migrate deploy`; `prisma migrate status`; read-only `INFORMATION_SCHEMA.COLUMNS` audit | Migration `20260825120000_remove_redundant_notification_columns` applied successfully; Prisma reports the database up to date; the removed-column query returned an empty result. | `test:db` was not run against production because it creates and deletes business data. A separate test database is still required before SCH-01 can become `COMPLETE`. |
| 2026-08-26 | SCH-01 verification attempt | `pnpm test:db` (14/14); read-only `INFORMATION_SCHEMA.COLUMNS` audit; `prisma migrate status` | The DB suite passed 14/14 and the read-only audit returned no removed columns; Prisma reports the database schema up to date. | Prisma resolved the datasource to Aiven production, so this write-test run is not accepted as SCH-01 evidence. Test/staging datasource is still required; no further production write tests should run. |
| 2026-08-25 | CFG-04 | Targeted 19-key RabbitMQ env audit; `pnpm --filter backend typecheck`; verify-change dry-run; `git diff --check` | Passed. RabbitMQ is enabled in the example; URL remains blank; IN_APP and EMAIL use distinct queues/DLQs; topic bindings and exact direct-DLX failure keys are consistent; Firebase is documented only as a future realtime transport behind IN_APP. | Configuration and architecture documentation only; no RabbitMQ or Firebase publisher/consumer runtime exists. Full verify was not run because the dirty worktree also selects an unrelated frontend build. |
| 2026-08-25 | SCH-02 source implementation | Prisma format/validate/generate; backend typecheck/build/unit; rename-only source audit; `prisma migrate status`; verify-change dry-run; frontend build; `git diff --check` | Source/schema/docs use `outbound_message_id`; migration contains only `CHANGE COLUMN`; provider-ID behavior and logical outbox relationship are unchanged; unit suite passed 75/75; selected backend/frontend gates passed. Migrate status found exactly one pending migration: `20260825130000_rename_notification_outbound_message_id`. | Production migration was not deployed. Database integration was not run because only the Aiven production datasource is configured. |
| 2026-08-25 | SCH-02 Aiven deploy | `prisma migrate deploy`; `prisma migrate status`; read-only `INFORMATION_SCHEMA.COLUMNS` audit | Migration `20260825130000_rename_notification_outbound_message_id` applied successfully; Prisma reports the database up to date; audit returned only `outbound_message_id varchar(255) NULL` and no old column. | Any backend process still running the old Prisma client must be restarted or deployed with the new source before it accesses notification deliveries. No production write test was run. |

| 2026-08-26 | MQ-01 | Prisma validate; backend typecheck/build; notification Rabbit unit tests (7/7); Rabbit integration harness (1/1 pass using configured AMQP URL); Rabbit/MariaDB CAS integration (notification-rabbit-cas.integration.test.ts, 1/1); full backend unit suite (82/82); verify-change dry/full; git diff --check; prisma migrate deploy/status; read-only schema audit; EXPLAIN; Docker availability check | Passed. Migration 20260826090000_add_rabbitmq_delivery_leases applied successfully; database is up to date; correlation_id_snapshot is varchar(64) NULL; delivery and outbox claim queries use the intended indexes with Using index; publisher reclaim/heartbeat CAS, consumer transition/attempt accounting, duplicate CAS protection and stale PROCESSING reclaim pass. | Rabbit and MariaDB CAS evidence is complete. CAS test used the local it_asset_tracking database and cleaned up its test rows; no production business-data write test was run. |
| 2026-08-26 | SCH-01 local verification | Local target `localhost:3306/it_asset_tracking`; `prisma migrate deploy`; focused `notification-outbox.integration.test.ts` (1/1); `test:notification:rabbit:cas` (1/1); read-only `INFORMATION_SCHEMA.COLUMNS` audit | Five pending migrations applied successfully, including `20260825120000_remove_redundant_notification_columns`; focused outbox and Rabbit/MariaDB CAS tests passed; removed-column audit returned no rows. | Local test data was cleaned by test hooks. No further Aiven write test was run. |
| 2026-08-26 | MAT-01 rich snapshots and approval summary | `pnpm --filter backend typecheck`; `pnpm --filter backend build`; `pnpm --filter backend test`; `pnpm --filter backend test:db`; `pnpm --filter backend test:notification:rabbit`; `pnpm --filter backend test:notification:rabbit:integration`; `pnpm --filter backend test:notification:rabbit:cas`; verify-change dry/full; `git diff --check` | Passed. Unit 89/89, DB 14/14, Rabbit unit 7/7, Rabbit topology 1/1, MariaDB CAS 1/1; lifecycle and outbox focused tests passed with rich snapshots, one Approve All summary event/delivery boundary, partial-success state preservation, and `html_body_snapshot` null. | No real mailbox receipt is asserted; the existing manual SMTP mailbox gate remains DOC-04. |
| 2026-08-26 | MAT-01 html delivery column removal | `prisma validate`; `prisma generate`; backend typecheck/build/unit; source audit; `prisma migrate status`; verify-change dry/full; `git diff --check` | Passed. Unit 90/90; schema validation and generated client succeeded; source has no runtime HTML delivery-column references; legacy SMTP fallback rebuilds branded HTML from title/text; verification selected backend typecheck and frontend production build. Prisma reports the new drop migration as pending on the configured Aiven datasource. | Migration was not applied to production and DB integration tests were not run because no separate local test credentials are configured. Apply the forward migration only against an approved staging/local/production deployment target. |
| 2026-08-27 | MAT-01 html deep-link rendering | `pnpm --filter backend exec tsx --test --test-concurrency=1 tests/notification-delivery.test.ts`; `pnpm --filter backend run typecheck`; `node <bigin-verify-change>/scripts/verify.mjs` | Passed. Notification delivery tests 21/21; backend typecheck passed; verify-change backend typecheck and frontend production build passed. HTML renders the deep-link only as the branded button while plain text retains the fallback URL. | No runtime/API/schema change; mailbox receipt remains covered by the existing manual SMTP gate. |
